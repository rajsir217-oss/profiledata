steps:
  # Build the container image
  - name: 'gcr.io/cloud-builders/docker'
    args:
      - 'build'
      - '-t'
      - 'gcr.io/$PROJECT_ID/matrimonial-frontend:latest'
      - '-f'
      - 'Dockerfile.prod'
      - '--build-arg'
      - 'REACT_APP_API_URL=https://matrimonial-backend-458052696267.us-central1.run.app/api/users'
      - '--build-arg'
      - 'REACT_APP_SOCKET_URL=https://matrimonial-backend-458052696267.us-central1.run.app'
      - '.'
  
  # Push the container image
  - name: 'gcr.io/cloud-builders/docker'
    args: ['push', 'gcr.io/$PROJECT_ID/matrimonial-frontend:latest']
  
  # Deploy to Cloud Run (cost-optimized)
  - name: 'gcr.io/google.com/cloudsdktool/cloud-sdk'
    entrypoint: gcloud
    args:
      - 'run'
      - 'deploy'
      - 'matrimonial-frontend'
      - '--image'
      - 'gcr.io/$PROJECT_ID/matrimonial-frontend:latest'
      - '--region'
      - 'us-central1'
      - '--platform'
      - 'managed'
      - '--allow-unauthenticated'
      - '--port'
      - '8080'
      - '--memory'
      - '256Mi'
      - '--cpu'
      - '1'
      - '--max-instances'
      - '2'
      - '--min-instances'
      - '0'
      - '--concurrency'
      - '100'
      - '--cpu-throttling'

  # Cleanup old images (keep only 3 most recent)
  - name: 'gcr.io/google.com/cloudsdktool/cloud-sdk'
    entrypoint: bash
    args:
      - '-c'
      - |
        # List all image digests except the 3 most recent, then delete them
        gcloud container images list-tags gcr.io/$PROJECT_ID/matrimonial-frontend \
          --format='get(digest)' --sort-by='~timestamp' \
          | tail -n +4 \
          | xargs -I {} gcloud container images delete gcr.io/$PROJECT_ID/matrimonial-frontend@sha256:{} --quiet --force-delete-tags 2>/dev/null || true

images:
  - 'gcr.io/$PROJECT_ID/matrimonial-frontend:latest'
