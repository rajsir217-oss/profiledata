MONGODB_URL=mongodb://localhost:27017
DATABASE_NAME=matrimonialDB
SECRET_KEY=your-secret-key-change-this-in-production
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30

# PII Encryption (REQUIRED for production)
# Generate key with: python crypto_utils.py
ENCRYPTION_KEY=your-encryption-key-generate-with-crypto-utils

FRONTEND_URL=http://localhost:3000
BACKEND_URL=http://localhost:8000
UPLOAD_DIR=uploads

# Email/SMTP Configuration (for notifications)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASSWORD=your-app-password
FROM_EMAIL=noreply@l3v3l.com
FROM_NAME=L3V3L Dating

# SMS Configuration (Choose ONE provider or set to 'auto')
# Options: simpletexting, twilio, aws_sns, auto
# RECOMMENDED: simpletexting (best for US/Canada, clean API, no opt-out issues)
SMS_PROVIDER=simpletexting

# SimpleTexting (PRIMARY PROVIDER) ✅
# Get credentials at: https://app2.simpletexting.com/ → API & Webhooks
SIMPLETEXTING_API_TOKEN=your-api-token-here
SIMPLETEXTING_ACCOUNT_PHONE=8338611131  # Your SimpleTexting number (10 digits, no +1)

# Twilio (Alternative)
TWILIO_ACCOUNT_SID=your-account-sid
TWILIO_AUTH_TOKEN=your-auth-token
TWILIO_FROM_PHONE=+1234567890

# AWS SNS (Alternative - 1M free/month)
AWS_ACCESS_KEY_ID=your-access-key
AWS_SECRET_ACCESS_KEY=your-secret-key
AWS_REGION=us-east-1

# App Configuration
APP_URL=http://localhost:3000
ENVIRONMENT=development  # development or production (enables mock OTP codes in dev)
